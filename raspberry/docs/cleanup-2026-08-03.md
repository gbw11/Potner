# 저장소 정리 기록 — 2026-08-03

전체 폴더를 훑어 구조 개편(루트 → `cli/`·`config/`) 이전 잔재와 병렬 하네스 부산물을
정리했다. **여기 적힌 파일은 이미 검증·정리 끝난 상태이므로 다음 정리 때 다시 볼 필요 없다.**
검증: `pytest tests/` 299 passed 2 skipped(정리 전과 동일), `compileall` 통과,
`main.py --once` mock 사이클 정상, cli 4종은 `python cli/x.py`·`python -m cli.x` 양쪽 실행 확인.

## 삭제한 것

| 대상 | 이유 |
|---|---|
| `scripts/capture_photo.py` | `cli/capture_photo.py` 로 이관된 구버전 사본 |
| `scripts/capture-and-pull.ps1` | `python -m cli.capture_photo` 가 같은 일을 함. 죽은 경로(`~/sensor-collector`) 참조 |
| `scripts/pull-from-pi.ps1` | scp 덮어쓰기 동기화 — git 배포로 대체됨. 죽은 경로 참조 |
| `scripts/send_climate_to_spring.py` | 어디서도 참조 없음. 존재하지 않는 config 키(`spring.url`) 사용. MQTT 텔레메트리로 대체됨 |
| `src/potner_*` 6개 디렉터리 | Robot 브랜치 잔재 — 소스 없이 `__pycache__`/런타임 데이터만 있었음. 소스는 Robot 브랜치들에 커밋돼 있음 (`potner_llm` 은 `Robot-feature/implement-response-validation`) |
| `voice-chat-server/` | 동일 — `__pycache__` 만 있었음. 소스는 `Robot-develop` 에 커밋됨 |
| git worktree 4개 (`.claude/worktrees/*`) | 병렬 하네스 종료 후 잔존. 전부 clean 상태에서 제거 |
| 브랜치 9개 (`stream-b-capture-pipeline`, `worktree-agent-*` 8개) | 4개는 HEAD 에 머지 완료, 4개는 타 브랜치 기존 커밋을 가리키는 포인터(고유 커밋 0개 — `git branch --contains` 로 확인) |

## 고친 것

| 파일 | 내용 |
|---|---|
| `cli/water.py`·`fan.py`·`plant.py`·`capture_photo.py` | `python cli/x.py` 직접 실행 시 `ModuleNotFoundError: src` — 루트 경로 부트스트랩 추가 (`-m` 실행만 되던 상태였음) |
| `cli/capture_photo.py` | 원격 경로 `~/sensor-collector` → `~/S15P11E104` |
| `scripts/deploy-to-pi.ps1` | 전면 재작성 — 존재하지 않는 루트 파일(scp) 복사 → git push(sync-tmp ref 경유 ff-merge) 방식 |
| `scripts/pull-camera.ps1` | 원격 경로 갱신 |
| `Jenkinsfile` | `plant.py`(없는 파일) → `cli`, `PI_DIR` 기본값 갱신, rsync 에 `.env`·`.claude`·`sensor-collector/` 보호 exclude 추가 |
| `README.md` | 루트 `plant.py`/`capture_photo.py`/`config.yaml` 시절 명령·배포 절차 전부 현행화 |
| `docs/potner-mqtt-pi-handoff.md` | ACL 차단 → 해결(2026-08-03 촬영 왕복+업로드 실측)로 갱신, result 토픽 규약 반영, 체크리스트 현행화 |
| `CLAUDE.md` | 삭제된 잔재 언급을 일반화된 경고로 교체 |

## 남긴 것 (지우면 안 됨)

- `data/` — 런타임 상태. 특히 `water_history.json` 은 과급수 방지 24h 누적 상태
- `scripts/publish_climate_mqtt.py` — 브로커 왕복 진단 도구 (ACL 조용한 거부 검증용)
- `scripts/setup-pi-ssh.ps1`, `scripts/run_pi_collector.sh`, `systemd/` — 현행 배포 경로
- `scripts/__init__.py` — `python -m scripts.x` 실행 스타일 유지용
- `.claude/agents/*.md` 4개 — 병렬 스트림 재가동 시 필요
- Pi 의 `sensor-collector/` 폴더 — gitignore 대상만 남은 잔재. git 이 조용한 게 정상

## 알아둘 것

- `pytest.ini` 에 `addopts = -q` 가 있어 명령줄에 `-q` 를 또 붙이면 `-qq` 가 되어
  최종 요약줄("N passed")이 안 보인다. 버그 아님 — `python -m pytest tests/` 로 실행할 것.
